//Una clase Serie con todos los atributos que le permita manejar los datos de una serie (10%)
export class Serie{
    constructor(public id:number, public name: string, public channel: string, public seasons: number,
                public description: string, public chLink: string, public image: string)
    {
        
    }
}